#import "OcValProcBase.h"


//! All this does is allows working with integer Point, Size, and Rects via links, but still uses real-based NSPoint, NSSize, & NSRect as storage
@interface OcValProcIntNSSize : OcValProcBase
@end
